# 2026-09-12 可空列增量验收

基线 `d102648` 上的未发布修改，版本仍为 `0.3.18`。本批处理新转换中已有 null 样本的普通叶子列；前一批公共 API 和表格提示修改见[首批记录](2026-09-12-incremental.md)。未重新执行已完成的历史审查。

## 修改范围

- schema 保留初始 null 与缺失单元格的区别，并为有 null 样本的普通列生成 `jsonScalarOptional` 类型。
- 模板、DDL、共享读写模块统一 JSON 标量编码；支持空字符串、null、数字、布尔、叶子删除、新行插入及无变化写入去重。
- SQLite 对解码值继续执行范围与枚举约束。提示词按表合并编码说明，SQL 示例遵守编码。
- 旧布局不迁移。仅声明 nullable、运行后首次出现 null、可空 VWD/对象列及无 null 样本的稀疏列仍待处理，完整约定见[数据映射](../data-model.md#可空普通列)。

## 自动化和构建

Node `22.23.2`。新增 `test/nullable-columns.js` 的 5 条行为回归；初始往返、动态/嵌套关系、新旧编码、脚本删除/新增/重复写及非法值拒绝均覆盖。SQLite 用真实 Python sqlite3 建表、插入初始行与默认行，验证范围和枚举仍拒绝非法值。

先运行相关读写用例，再在本批源码稳定后运行一次全量：**316 通过，0 失败**。沙箱内 Python 子进程曾启动超时，随后在允许派生进程的环境定向通过；最终全量也在该环境通过，未把超时记为跳过或通过。

构建、`node --check index.js`、版本一致性及根目录/隔离安装目录 `index.js` 比较通过，生成包为 1,353,182 字节。

## 真实宿主

命令：`node test/real-host.js --only=nullable --keep-server`。使用[首批记录](2026-09-12-incremental.md#定向宿主场景)中的固定宿主，SP commit 为 `5c53f795832b194ec4baa74135e8ebd950122438`，不代表对参考库新 HEAD 的完整认证。

运行 `2026-09-12T13-07-00-979Z`，native 和 SQLite 均通过：

- 初始 null 经转换、初始化和重载保持 null。
- 有值再次改成 null、改成空字符串、0、false 和删除键，重载后分别保持原语义。
- 动态行中已有行更新、新行插入及原本缺失的 nullable 字段保持正确；展平嵌套字段同样保持。
- 浏览器错误列表为空。测试不调用真实模型。

## 后续业务事件探针

运行 `2026-09-12T13-17-30-327Z`，复用隔离环境，以同一个异步 `VARIABLE_UPDATE_ENDED` 监听器在金币为 42 时将生命改为 73。

| 入口 | native | SQLite |
| --- | --- | --- |
| `Mvu.parseMessage` 返回的生命 | 73 | 73 |
| SP 公共 CRUD 提交后，监听器执行次数 | 1 | 1 |
| SP 表格中的生命 / 重载后的生命 | 100 / 100 | 100 / 100 |

这是 SP 公共 CRUD 的真实提交回调探针，未运行自动填表模型，也未验证文本结果提交后的重载。它确认事件参数修改未进入这条提交管线，**该问题尚未修复**。后续应先建立事务来源和业务变换边界，不能在每次前端广播后无条件补写；历史回放、手动刷新与新业务提交不能混用。

字符/token 与模型质量 A/B 不属于本批验证范围。
