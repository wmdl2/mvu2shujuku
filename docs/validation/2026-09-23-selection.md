# 表格反选与报告文案验证（2026-09-23）

- 删除“应用到全部”按钮；全选与反选只操作筛选后的可见行，隐藏行的选择保留。“应用到所选”仍覆盖所有已选表，包括隐藏已选。空列表禁用全选和反选。
- 人工项改称“兼容性待确认”，说明它是转换完成后的兼容提醒；全部条目、展开状态与完整报告保持不变，未把语义差异改写成无影响。
- `node test/run-tests.js --grep '结果设置'`：5 通过、0 失败。
- `node test/frontend-layout.js`：真实视图工厂通过反选、筛选隐藏、全选状态、空列表禁用、所选表更新和报告文本安全检查；314/390/760px 均无横向溢出，页面异常 0。只在一个宽度运行行为场景，避免重复相同行为测试。
- 宿主 UI 夹具同步新按钮；报告断言使用明确注入提醒的样本，避免普通合成卡无提醒时空指针。`node --check test/frontend-host.js` 通过。
- 构建、生成包语法和 `git diff --check` 通过；index.js SHA-256 `fee59cde74e6291bf818f6bc7bb3c206ad0718171ae5ca3ee951912093883fbc`。
- 本批只修改界面选择行为和提示文案，未启动隔离酒馆、未重复全量。保存复核的 491 项全量及宿主证据见[保存复核验证](2026-09-23-save-verification.md)，其受测构建与本批分别记录。

版本保持 0.4.0，此改动尚未发布。生成模板的字段、注入设置和保存协议没有随反选操作改变。
