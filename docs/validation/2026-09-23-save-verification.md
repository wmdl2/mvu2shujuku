# 保存后脚本复核（2026-09-23）

旧流程要求成功定位新卡并调用宿主字段接口，未满足任一条件就显示同一条警告；调用成功后也未再读取磁盘数据。宿主字段接口可能只打印 HTTP 错误而正常返回，因此这既可能误报，也可能漏报。

## 调整

- 保留按头像、转换标记定位与官方字段同步，不按同名卡补写。
- 同步后按唯一头像读回完整卡，核对转换标记与酒馆助手数据；对象键顺序不影响比较，数组顺序和脚本内容必须一致。
- 列表滞后、缺少刷新或写入接口时，若新卡实际读回一致，也确认成功；无唯一身份不猜测目标。
- HTTP/响应结构错误、标记不一致和脚本差异分别提示。复核失败不伪装成创建失败，也不重复创建角色卡。
- 每次保存增加一次完整卡读请求，只发生在用户保存操作，不进入聊天或变量更新热路径。

## 验证

- 定向回归 11 通过、0 失败；新增 6 项使用真实保存和完整卡读取实现，覆盖无字段接口、列表滞后、无唯一头像、静默写入失败、无效响应与标记不符。
- 隔离宿主：SillyTavern 1.18.0 / `8172dcd`，酒馆助手 4.11.0 / `0df1a263ba0d9f7055d5ecf7d92fd51a88a6ba0d`，SP 固定 `5c53f795` / 9.2.5。助手取自本机已安装版本，不声称最新上游。
- runId `2026-09-23T02-35-26-459Z`：开启角色浅加载，PNG 转换与保存通过；观察到 create → all → get → merge-attributes → get，最后读回一致，页面异常 0。仅复验保存，不重跑数据库行为。
- 受测包 SHA-256 `62c56f54af6534389bcda28d186ae9984bd813349406709b753af4022e4e627d`，1,673,291 字节；构建与语法检查通过。
- 全量 **491 通过、0 失败**，退出码 0；这是保存复核批次的固定构建，后续界面反选改动单独验证，不混用指纹。
- 用户在原先稳定出现警告的环境复试，反馈“已读回确认酒馆助手脚本与转换结果一致”。这证明本次保存数据一致；未确定其环境为什么跳过额外字段同步，不能归因于助手升级。隔离环境在修改前的普通/浅加载两种配置下均未复现原警告。

私有卡仅用于本地隔离验证，正文与截图不纳入仓库。公开回归使用合成数据；隔离服务已退出。版本仍为 0.4.0，此改动尚未发布。
