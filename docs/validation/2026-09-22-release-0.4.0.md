# v0.4.0 发布验证

本版本集中交付 9 月 12–23 日已经分批实施、验收的兼容与界面改动。
9 月 22 日完成控件样式、升级说明与版本号收尾；9 月 23 日追加保存后脚本读回复核、
列表反选和兼容提醒文案。以下区分各批次的验证证据，未将早期构建当作最终包。

## 升级范围

- 新扩展继续读取已支持的旧卡布局，并保留旧桥退出交接协议；升级不自动迁移旧聊天或合并原有表格。
- 已有转换卡无需为了界面更新而重新转换。选择完整 JSON 等新增布局时，再转换原卡并建立匹配的新聊天。
- 此前已完成的可空容器/记录、动态说明、pair 编码、写入楼层和批次保护，范围及宿主证据按[文档索引](../README.md)查阅；不把保留边界写成任意 MVU/Zod 全兼容。

## 9 月 22 日基线检查

- 源码 `VERSION`、构建 manifest 与生成包统一为 **0.4.0**；构建和 JavaScript 语法检查通过。
- 该批全量 **485 通过、0 失败**，进程退出码 0；运行后再次核对 88 份冻结文件，指纹全部一致。暂存检查随后清理了两份新增源码/测试的空白并重建产物，逐字节对照确认只删除空白行缩进和测试文件末尾多余换行，语法复验通过，未重复全量。
- 使用真实视图工厂和样式检查 314px 窄侧栏、390px 手机、760px 宽面板：编辑器及内部列表无横向溢出，数字输入框的背景、文字、边框、圆角、内边距、字号和高度一致，解释区块已移除，页面异常 0。
- 该批构建 `index.js`：1,669,893 字节，SHA-256 `25b711ce9350ce531584b97bcf2e38f6d99a31c7408bf661b744cee0e15e46f7`。源码、测试及构建共 88 份文件在检查前冻结指纹。
- 本次没有重新执行已通过的数据库宿主场景。多选、下载、配置复用和保存仍以[前端验收](2026-09-22-frontend.md)为依据；本次只补受影响的界面检查，完整 JSON 等写入行为沿用各批次固定构建证据。

```sh
node build-extension.js
node test/frontend-layout.js
node test/run-tests.js
git diff --check
```

截图可通过 `MVU_TEST_FONT` 指定本地中文字体；字体与截图均不打包进扩展。
提交包含公开源码、构建、测试与文档，本地审查/缓存/隔离聊天数据不纳入 Git。

## 9 月 23 日发布前补充

- 保存复核改为读取实际落盘角色卡并比较转换标记与酒馆助手脚本；11 项定向、491 项全量和隔离宿主保存场景通过，用户原告警场景复试成功。该批受测构建与范围见[保存复核验证](2026-09-23-save-verification.md)。
- 随后的 UI 批次用反选当前列表替代应用到全部，并将人工项标题改为兼容性待确认；5 项相关测试、真实组件交互和三种宽度检查通过，未重复全量或完整宿主验收。见[多选界面验证](2026-09-23-selection.md)。
- 最终发布包 `index.js`：1,673,417 字节，SHA-256 `fee59cde74e6291bf818f6bc7bb3c206ad0718171ae5ca3ee951912093883fbc`，版本仍为 **0.4.0**。491 项全量对应保存批次构建，最终包另叠加已定向验证的 UI 改动。
- 隐藏列策略维持原状；任意 Zod 执行、原 MVU 更新驱动等保留边界以 [COMPATIBILITY.md](../../COMPATIBILITY.md) 为准，不宣称完整等价。
- 本次最后收尾只合并发布日志与验证索引，未改变受测程序。
