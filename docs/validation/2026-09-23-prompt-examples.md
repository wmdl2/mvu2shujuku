# 强制更新提醒与 SQL 示例验证

## 改动与边界

- `_强制更新提醒` 的各条字段和触发条件保持原文，共用“强制更新提醒（按各项触发条件执行）”标题，不再逐条添加“每次回复必须维护”。
- 双模式与 SQLite 模板保留具体 SQL 示例，native 专用模板不附 SQL。双模式明确要求 native 忽略 SQL 示例并遵循宿主原生协议；示例用途及禁止照抄的说明每表 note 仅一次，无示例的表不添加此说明。
- 示例继续使用既有默认值、类型、枚举、范围和完整关联定位；JSON UPDATE 优先修改安全的已有路径，缺少安全路径时不虚构示例。表格数据、DDL 与业务触发条件保持不变。
- 修改影响后续生成的转换模板，不自动改写已有卡或聊天的表格说明。没有真实模型对照实验，不宣称填表准确率提高了某个幅度。

## 验证

- 相关回归 **20 通过、0 失败**；本批一次全量 **492 通过、0 失败**。涵盖模式适用说明、条件保真、关联定位、隐藏列及 VWD 默认边界；双模式与 SQLite 的数值、枚举、布尔和可空 UPDATE 示例在 SQLite 中实际执行。
- 原 VWD 冻结文件未重生成；对照只排除本次明确新增的 SQL 示例和单行适用说明，原业务 note、数据、DDL、触发条件和布局仍对照冻结结果。跨模式测试保留数组行号等必要差异。
- 构建、生成包语法检查、差异空白检查通过。最终 `index.js` 为 **1,674,039 字节**，SHA-256 `a022f19fc850df6889d3fead74a2f99851e133b08f04ec1f44c946eace4c526b`，版本 **0.4.0**。提交 `f2a9b73` 中受测文件与冻结指纹一致。
- 本批不涉及宿主写入协议或接口变更，未重复隔离酒馆验收；宿主版本和既有填表边界见[关联提示验收](2026-09-15-prompt-guidance.md)。

```sh
node test/run-tests.js --grep '强制更新提醒|表格协议|特殊操作提示|SQL.*示例|JSON局部更新|默认关闭模板|顶层数值'
node build-extension.js
node --check index.js
node test/run-tests.js
git diff --check
```

## 提示成本

使用公共 `test/prompt-guidance-card.js`，对照 `3f4d1be` 与本批生成器，借助 `test/measure-template-delta.js` 统计 5 张表各提示字段的 token 总和（o200k_base）：**1,966 → 2,613，增加 647**。表名、顺序、数据与 DDL 对照一致。该值是模板字段库存，包含各操作说明，不是完整请求、实际账单或模型质量测量。

`node test/measure-json-containers.js` 的完整 JSON 样本从 2 条扩展至 200 条时，DDL 均为 128 token，操作说明均为 204 token，note 均为 910 token；初始大 JSON 没有被重复复制进这些部分。
