# 普通脚本写入的楼层归属诊断

本页保留修复前的诊断结果；此缺陷现已修复，当前实现与验收见[楼层归属修复](2026-09-16-write-floor.md)。

当前已确认一项兼容缺口：普通 `Mvu.replaceMvuData` 的保存楼层可能与需要整批导入的数组
更新不同。新 AI 回复尚无表格记录时，普通 CRUD 修改的是更早的已有记录；删除新回复
不会撤销这份旧楼更新。**本次完成定位，未修改产品写入逻辑。**

## 最小对照

公开合成卡，开场金币 10；新增用户楼 1 和 AI 回复 2；写入金币 42；删除楼 1–2 并刷新。
同时保存修改前后各楼的持久化帧，确认变动实际发生在哪里。

| 入口 | native 保存楼层 / 最终金币 | SQLite 保存楼层 / 最终金币 |
| --- | --- | --- |
| 普通 MVU 写入，新回复无表格记录 | 0 / 42 | 0 / 42 |
| 直接 SP.updateCell，同样无记录 | 0 / 42 | 0 / 42 |
| 普通 MVU 写入，新回复先有记录 | 2 / 10 | 2 / 10 |
| 多步数组修改，走整批导入 | 2 / 10 | 2 / 10 |
| 新回复中的 UpdateVariable | 2 / 10 | 2 / 10 |

受测 SP `5c53f795`（9.2.5）；SQLite 在导入卡前切换，刷新后检查实际 SQL getter，未把 native
回退计为 SQLite。源码参考 `8646e5cc` 的 `findTableLatestFloor` →
`getLatestTableAppendMessageIndexFromChat_ACU` 与实测一致：普通 CRUD 优先选择已有表格记录的 AI 楼。

```sh
node test/real-host.js --only=rollback
# 定向补测，不重复其它组合：
node test/real-host.js --only=rollback --rollback-modes=sqlite --rollback-cases=mvu-no-frame
```

实现位于 `test/rollback-host.js`，由真实宿主 runner 调用，不使用私有角色卡。
证据来自分批运行：`2026-09-16T04-07-53-025Z`（native 四种场景；消息夹具用编辑替代新回复而超时），
`2026-09-16T05-17-44-777Z`（纠正后 native 消息及普通写入），
`2026-09-16T05-19-51-681Z`（SQLite 五种场景）。后两次未捕获页面异常均为 0。
首轮夹具失败不计为产品缺陷；诊断完成不等于保存归属已经修复。

## 后续修复边界

统一普通公共写入的当前 AI 回复归属；绑定消息身份并覆盖异步取消，复用正式快照提交管线，
不手写 SP 历史帧。是否能可靠确定当前隔离作用域已有本楼记录，是保留后续差量写入的前提。
需要先验证该条件或衡量每批快照成本，再实施；不能因回退问题让所有小修改无条件扩大写入成本。
开场初始化、历史 message_id 的既有支持范围及 VWD 默认关闭状态保持现状。

SQLite 普通写入另以 `2026-09-16T05-23-17-668Z` 补测完整字段就绪后的读回，pair 值正确、页面异常 0；避免将加载过渡状态计为损坏。产品包与调查前逐字节一致，语法及差异检查通过；隔离服务已关闭。
