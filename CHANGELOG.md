# 更新日志

## 未发布

- 用“反选当前列表”替代“应用到全部”；隐藏的已有选择保持不变。报告的人工项标题改为“兼容性待确认”，保留全部说明及完整报告，避免误读成转换失败。

- 修正保存角色卡后的脚本复核：实际读回并比较脚本，避免把额外字段同步不可用误报为保存异常；读取失败、转换标记不符和数据差异分别说明。

## v0.4.0（2026-09-22）

本次集中完善 MVU 语义兼容、表格提示与运行时写入，并整理转换界面。新扩展继续支持已有转换卡的旧布局，不自动迁移旧聊天；新增布局能力在重新转换并新建聊天后生效。

发布检查及各批次证据见[发布验证](docs/validation/2026-09-22-release-0.4.0.md)。

- 统一数字输入框、搜索框和下拉框的主题、边框与焦点样式，移除界面中的世界书注入解释区块。

- 优化转换界面：优先展示人工处理项和注意事项，详细记录与高级选项折叠；表格支持搜索、多选批量设置数值及世界书注入开关，设置随转换配置复用，已有聊天不自动改变。

- 新增完整 JSON 容器转换选项：合并所选顶层容器的子表，无状态列地保存缺失/null/空容器及嵌套 pair 说明；保留规则与 SQLite 局部更新。去除 DDL 对大 JSON 初值的重复，并修复刷新切换模式时复用旧列的问题。见[范围与验收](docs/validation/2026-09-22-full-json-containers.md)。
- 补齐独立 YAML 占位键下的嵌套规则；旧楼业务修正先规划，拒绝会部分落库的多步提交；新动态说明布局支持单例数字/布尔 pair，物理列类型保持不变。见[范围与验收](docs/validation/2026-09-22-compatibility-followup.md)。
- 明确声明可空的动态记录逐条使用完整 JSON 列，保留字段规则和关联身份；补齐只有子记录的身份创建，修复同名不同归属的新增误拦截及顶层数组被误建子表。见[可空记录验收](docs/validation/2026-09-21-nullable-records.md)。

- 明确声明可空/可缺失的顶层对象、数组、字典保留为完整 JSON 列，区分缺失、null 与空容器；保留内部字段说明、更新规则和固定路径范围/枚举检查，不添加状态列。见[整组空值验收](docs/validation/2026-09-16-container-presence.md)。

- 新转换在宿主能力满足时支持单例表的 VWD 动态说明：隐藏保存、填表时读取，支持初始空说明、清空及历史回退。含模板标记的说明使用可逆 JSON 表示，避免再次执行；缺少匹配模板或 EJS 桥时拒绝说明写入。见[范围与验收](docs/validation/2026-09-16-vwd-prompt.md)。
- 修复自有脚本成功写库后，旧的提交读窗口仍返回上一份变量的问题；成功后立即同步最终快照，避免短聊天中最多 5 秒的旧读值。

- 加强当前回复的脚本混合更新：先在内存完成真实写入规划，多次宿主调用合为一次正式导入，单次单元格/行更新保留差量。提交拒绝不留下先完成的独立步骤，仍保留宿主已保存后返回失败的边界。见 [批次验收与成本](docs/validation/2026-09-16-atomic-batch.md)。

- 普通脚本写入绑定当前 AI 回复，修复删楼后修改仍留在较早楼的问题；新楼首次快照提交，可靠时保留后续差量，并取消来源已变化的旧任务。过滤未变化 pair 的重复写入。见 [楼层归属验收](docs/validation/2026-09-16-write-floor.md)。

- 修复旧式 `[当前值, 说明]` 写入污染单元格及 SQLite hydrate 回退；补齐新增记录与布尔 pair 编码，保留可空值语义。见 [pair 写入验收](docs/validation/2026-09-16-pair-write.md)。

- 关联记录增加完整定位组合示例；数组区分来源内序号、native 整表行号和 SQL row_id。两种模式的定向填表、重载和删楼验收通过，见[本批验收](docs/validation/2026-09-15-prompt-guidance.md)及[成本增量](docs/benchmarks/prompt-2026-09-15.md)。
- 修复规则清洗误删普通业务指令和括号条件；转换已识别的 MVU 写法时保留增减值、重置值、超限限制与数组删除条件，无法确定含义时保留原文。见[业务规则验收](docs/validation/2026-09-15-business-rules.md)。
- 修复相同规则折叠导致独有字段描述丢失；按实际列名保留描述、单位、格式和条件，保留可见只读字段含义。提示词维护以完整性和安全为优先，见[本批验收](docs/validation/2026-09-14-prompt-safety.md)。
- 修复 get/list/val 与 entries 辅助扫描中的字符串、参数遮蔽和无关 data 误补列；保留合法回调和附属组读取。
- SQL 示例遵守枚举、数值范围、布尔和 JSON 编码；关联数组填写全部关联字段，并明确示例值不得直接照抄。INSERT 保留初始值/默认值策略，既有 JSON 路径说明保留。
- 修复前端扫描跨函数/脚本复用同名临时变量时误补表格列；按作用域追踪可识别别名，保留合法闭包和 HTML 脚本读取。规则迁移及关联提示的证据见[本批验收](docs/validation/2026-09-14-card-rules.md)。
- 修复规则 `check` 中单行 `op: …, value: …` 说明引发的 YAML 误解析；完整规则成功迁移后可移除旧条目，其他解析错误和混合业务仍保留。
- 填表提示统一使用「关联字段」并列出实际定位列，明确同步删除和标识值变更操作，减少父键/祖先键等混用；内部布局和物理列名不变。
- 可写 JSON 列增加 SQLite 路径更新说明与安全示例，双模式模板同时生效；移除强制整段重写的冲突文案，native 保留原有单元格更新方式。
- 明确固定 Zod 附属对象并回父表列，保留每条逻辑路径的范围、枚举、类型、只读和隐藏语义；动态记录及数组继续独立存储，不自动迁移旧聊天。
- JSON 路径执行、合并字段读写及重载/删楼已通过 SP 实机验收，范围与成本边界见 [JSON 与合表验收](docs/validation/2026-09-14-json-path.md)。
- 新转换支持 null 样本及 nullable/optional 声明的标量列、字段级可空 JSON 容器和有 null 样本的 VWD；区分 null、空串、缺失、0、false。普通文本清空不再恢复默认值；旧布局不猜测迁移。
- 修复公共 MVU getter 误拆普通两元素数组、getter/setter 不支持方括号与引号键路径，以及递归 setter 未等待异步监听器完成的问题。
- 无可用 iframe 事件发射器时，从 SillyTavern 上下文取得共享总线并等待其完成，修复只检查窗口全局而遗漏业务事件的问题。
- 新转换模板按逻辑父子关系聚拢相关表，保持 UID、layout 和行数据不变，不自动调整已有聊天顺序。
- 表格 note 不再自行编号；native 与双模式的触发说明移除生成的 SQL 示例，SQLite 专用模板保留，减少与宿主协议的冲突。
- SP 持久化业务提交按 V2 来源去重，等待异步结束监听并保存修正值；文本入口避免重复业务事件，回放与自身提交不反复计算。补齐每轮 display/delta 与早期事件取消。
- 按 SP 版本隐藏内部物理列，保留可见只读业务字段；精简重复列映射，补充关联操作规则和原始 initvar 重复进入填表请求的提示。
- 脚本删除/重命名父行时清理后代物理行；窗口枚举引入可失效缓存，保留晚加载、导航与切聊天处理。
- 综合清单的实施、实机证据和模型压力测量见[语义验收](docs/validation/2026-09-13-semantics.md)及[提示词测量](docs/benchmarks/prompt-2026-09-13.md)。

## v0.3.18（2026-09-11）

- 新转换卡只携带轻量登记桥，扩展晚加载时自动重试并显示等待提示；移除生成桥中的第二套运行时。历史桥退出交接保留兼容测试。
- 按输入解析、布局推导、EJS 转换、桥登记、宿主适配、会话与界面边界拆分源码，仍构建为单个 `index.js` 安装入口。
- 数组按位置规划最小差量；单操作走 CRUD，多操作一次原子导入。缺能力、并发变化和提交失败明确返回失败，不以反向覆盖补偿可能已持久化的提交。优化行索引与变更分组，补充 100／1000／10000 行基准。
- 修复 native 模式因 SQL getter 隐藏而无法写入；开场内存适配器支持数组原子导入，构造失败时不再持久化旧快照。
- 消息更新绑定来源消息与会话；快速删楼取消待写任务。消息更新以整表提交落到最新回复，避免手动 CRUD 把更新追加到首楼，导致删除与重生成无法回退。同内容的新回复重新执行，已有消息移动楼层不重复执行。
- 增加公开合成卡、可控时钟与行为回归，以及独立真实酒馆验收脚本 `test/real-host.js`。全量自动化 301 项通过；无私有卡副本 236 项通过、65 项附加回归跳过。真实宿主场景和版本证据见兼容清单。

## v0.3.17（2026-09-09）

- 顶层有限数字改用 REAL 数值列，默认值取原值，允许小数；提示与 SQL 示例直接操作数字，不再使用对象示例或 JSON CHECK。
- 兼容投影仍还原原始顶层数字路径；缺表默认值、补行与实际生成桥共用此语义，写入拒绝对象、字符串和非有限数字。旧 JSON 布局保持原读写方式，不自动迁移旧聊天。
- 正则仅在整个替换体为纯旧变量快照宏时删除；无按钮的状态栏、包含更新块或兼容 API 的业务脚本保留，保留前端的格式化宏接入数据库展示入口。
- 世界书不再凭显式更新标记或名称子串删除；业务 EJS、非专名混合条目和无法完整解析的规则保留并报告核对。初始化与明确的旧输出文档仍删除。
- 新增 5 项数值往返、生成桥及删除边界回归，修正旧测试中的宽泛删除假设；全量 275 项通过，实际命运点数 DDL 的默认值、小数写入与非法值拒绝经 SQLite 验证。

## v0.3.16（2026-09-09）

- 支持 `variables_update_rules` 英文规则外壳；TypeScript 纯字符串联合枚举按字段路径迁入表格提示与 CHECK，避免同名字段跨组污染。
- 已识别的专用规则条目从转换副本移除；修复 v4.3.2 卡任务关注度缺少“低／中／高”约束的问题。
- 保留包含脚本与交互控件的开局正则，修复 6.png 开局配置面板因含 `<UpdateVariable>` 被误删的问题。
- 保护前端 script 中 `/<%|%>/` 标签检测写法；扩展在提示词模板的 render HTML 阶段处理动态前端，避免 `Unexpected token '|'`，切卡或切聊天后旧保护回调失效。
- 新增 6 项回归，全量 270 项通过；两张实际卡修复前后分别为 33／14 表，表名与列名一致。动态前端与完整自定义开局仍待酒馆实机复验。
- 边界：JSON 标量表仍复用通用 JSON 默认值与对象示例；删除判定仍可能误删不含表单控件的显示前端、带显式 MVU 更新标记的混合业务条目。这些问题在后续审查中确认，本版尚未修复。

## v0.3.15（2026-09-08）

- 新生成的兼容桥提供退出协议：扩展晚加载时取消旧桥定时器、监听与待提交快照，等待在途调用和批次收尾，再启动扩展运行时。
- 旧 Mvu 引用和已排队回调不能在接管后继续写库或抢回全局对象；数据库 API 尚未加载的重试也纳入退出管理。
- 扩展登记抛错也不会回退启动第二套运行时；新增5项生命周期和实际桥交接回归。旧版本转换卡需要重新转换才能获得退出协议。

## v0.3.14（2026-09-08）

- 独立旧桥内联共用写入模块，删除重复的路径路由及差异写入算法，同一行多字段写入复用合并提交。
- 每个桥使用私有写入实例；最终写入失败返回 false，不广播成功，也不重新运行整套旧兜底。
- 统一 JSON 组写入语义：删除凭空对象变标量猜测默认回声的拦截，允许合法字符串、数字和布尔值；内容未变化仍跳过提交。
- 增加实际生成桥的合并写入与失败反馈回归。事件状态机和扩展晚加载交接仍待收敛。

## v0.3.13（2026-09-08）

- 提取 `src/table-codec.js`，扩展和旧卡桥共用表格投影、单元格类型转换与 JSON 容错逻辑，移除桥侧重复读表实现。
- 提取 `src/table-writer.js`，通过参数注入 API、JSON 解析、日志与模板缓存读取，保留核心写入接口及失败反馈契约。
- 每次读快照只建立一次表名索引，保留首次匹配、空表骨架、seedRows、关系子表及溢出字段语义。
- 构建自动内联共用工厂，无新增浏览器依赖或安装步骤；增加 11 项模块、桥和浏览器产物的一致性回归。
- 旧桥的事件与写入兜底仍保留，本轮不改变持久化流程。

## v0.3.12（2026-09-08）

- 转换分析与产物装配分离：应用配置、修改模板参数和合并表格时复用已有分析，只更新内嵌模板、桥与下载文件。
- 无改动刷新直接复用结果；仅切换 PNG/JSON 格式时只重新打包文件。转换规则变化自动回退完整转换。
- 保留初次输入快照、源角色身份、配置摘要和编辑器表对象引用；仅新增或修改的表格提示词执行迁移。
- 增加 8 项产物一致性回归及可重复的刷新性能基准脚本。

## v0.3.11（2026-09-08）

- 写入入口主动清理已过期的会话：切聊天通知迟到时，新写入不再继承旧 Promise；旧重试不会清空新会话数据。
- 统一切聊天、拒绝旧重试时的快照、定时器与 Promise 清理，避免共享读投影残留旧数据。
- EJS 各变量接口共用对象方法判定，保留带空白/注释的对象调用，并避免为每个调用复制完整源码前缀。
- 新增 5 项队列及 EJS 边界回归，使用可控定时器验证合并、取消和回调结算。

## v0.3.10（2026-09-08）

- 顶层数组按 JSON 类型编码往返；CRUD 失败停止后续写入，失败批次不再广播成功。
- 异步初始化、写队列和重试绑定原会话；加强同名角色缓存与数据桥注册归属校验。
- 保留混合业务脚本和仅名称相似的普通正则；EJS 改写跳过字符串、注释和正则字面量。
- 修复 JSON 文件字节导入、空世界书问候语初始化、数据桥下载内容及保存期间结果与头像串用。
- 增加不依赖私有卡的行为回归，按表和行预先分组差异更新。

## v0.3.9（2026-09-05）

- 修复表名宏静态化后，状态栏无法从 `stat_data.<当前用户>` 读取 `user表` 的问题：物理表名保持稳定，layout 逻辑路径在运行时按 SillyTavern 当前上下文解析宏。
- 扩展 owner 与卡内独立桥共用同一解析语义，读取和写回使用同一份运行时 layout。

## v0.3.8（2026-09-04）

- 表名中的宏改为转换时静态规范化：`{{user}}` / `<user>` 生成 `user表`，带参数宏的分隔符折叠为下划线；数据库结构名保持稳定，MVU 逻辑路径不变，规范化后重名自动追加序号。
- `_扩展数据` 现在复用现有下划线只读提示；它仍不出现在列定义和 INSERT/UPDATE 示例中。
- 同一字段拥有多条范围、枚举、格式、check 或字段说明时，「强制约束」改为字段名一次、规则缩进分组；单条规则仍保持单行。

## v0.3.7（2026-09-04）

- 修复保存转换卡后复核酒馆助手脚本时报 `deepClone is not defined`：UI 独立作用域现在使用自身的 JSON 副本写入 ST 官方 `writeExtensionField`，并通过真实保存流程回归测试覆盖。
- 兼容没有 `[mvu_update]` 前缀、但以“变量更新规则”“变量处理指令集”等专名存在的纯 MVU 世界书条目。规则约束先迁入数据库表备注，再从转换副本移除；含剧情或业务 EJS 的混合主控条目仍完整保留。
- 修复新聊天被 SP·数据库初始化竞态挂上其他角色卡 full checkpoint 后，当前卡模板永远不建表、JSONPatch 因跨布局保护而无法写入的问题。仅当聊天恰有一个非用户首楼、运行时明确存在当前模板之外的表且当前模板缺表时，才用当前卡模板重建；出现任何用户楼层后绝不自动覆盖。
- 异卡 checkpoint 恢复后按“新初始化”继续应用首楼 `<initvar>` / `<UpdateVariable>` / `<JSONPatch>`，不会误登记为旧聊天历史；兼容桥同样发布 fresh checkpoint 标记供扩展接管。

## v0.3.6（2026-09-04）

- 修复 SillyTavern 开启 `lazyLoadCharacters` 后，新建角色列表不携带 `extensions.mvu2shujuku` 导致保存后误报“未能使用 ST 官方字段接口复核”。现在先用 `/api/characters/create` 返回的唯一头像名定位新卡，再按需取完整卡校验 `converter + convertedAt`，同名旧卡不会被覆盖。
- EJS 中直接读取 `window.stat_data` / `globalThis.stat_data` 的写法现会改由数据库 helper 提供实时状态，不再依赖 MVU 注入的窗口全局。
- 修复 st-prompt-template 把多个世界书条目合并为同一 EJS 源码后，一个条目的 `const/let/class x` 与另一个条目的 `if (typeof x === 'undefined') var x = ...` 触发 `Identifier has already been declared`。转换器只对这一可精确识别的跨条目回退声明添加独立异步 EJS 作用域，并写入转换报告。

## v0.3.5（2026-09-02）

- 扩大魔法棒“刷新转换卡前端”的兼容范围：转换时只给明确读取 MVU/消息变量的 HTML 前端标记 `event/direct/control/reload` 能力，运行时依次使用原生重读入口、`VARIABLE_UPDATE_ENDED`、精确声明的刷新控件和受限 iframe 重载。
- 修复事件型内联状态栏已收到刷新事件、却仍提示“未找到前端”的误导反馈；现在会按直接、事件、控件、重载分别报告处理数量。
- 手动 reload 仅作用于带转换器标准标记的非主/宿主 iframe；普通 HTML、酒馆主页面和无关 iframe 不会被重载。事件型或直接重读型前端也不会再额外点击同窗口的刷新控件。
- 修复删除聊天楼层、切换 swipe 或增改消息后，SP·数据库已按历史回放完成表格变化、转换卡前端却仍显示旧状态：除 SP 通用表更新回调外，聊天历史变更现在会等待完整运行时快照确实变化后补发，兼容旧版 SP、重载窗口和回调时序竞争。
- 所有表更新通知统一驱动 `VARIABLE_UPDATE_ENDED`、`shujuku-table-updated`、已确认无副作用的内联重读入口和精确刷新控件；批量写抑制期间收到的最后回调会延迟重试。自动更新仍不执行整页 `body.load`/iframe reload，避免清空前端局部状态。

## v0.3.4（2026-08-25）

- 新增教程标准的酒馆助手 Zod Schema 静态迁移：解析传给 `registerMvuSchema(...)` 的 `z.object/z.record/z.array`、基础类型、`z.coerce.number`、枚举、描述、字面量 `prefault/default`，以及标准 `transform(value => _.clamp(...))` 范围；支持中英文 Schema 变量、`.shape` 展开、`extend/merge`。
- `z.record` 现在作为动态字典结构证据，空 InitVar 也能按 value Schema 建行表或嵌套关系表；键 Schema 的 `.describe(...)` 会作为业务键列名。`z.array` 继续按转换器既有规则生成有序数组表。Zod 默认值用于补齐缺失初始字段及数据库列默认值，已有 InitVar 值优先。
- 自定义 `transform/refine/superRefine/preprocess/pipe`、未知组合类型和无法解析的注册对象不再静默丢失，转换报告会列出字段路径和原表达式。转换器不执行角色卡 JavaScript。
- 删除不可见的纯外部 `data_schema` 启动器时新增显式人工核对项：继续阻止旧 Zod 与数据库双轨运行，同时不再把无法读取的远程 Schema 暗示为已完整迁移。
- 兼容清单补齐教程附录中的剩余 Zod 边界（`int/regex/or/union/templateLiteral/partialRecord/intersection`），明确区分“按 InitVar 实值仍可建表”和“空结构唯一来源需人工补表”，避免把静态子集解析器误称为完整 Zod 执行器。
- `_.clamp` 映射为表格范围提示和用户可选的 DDL `CHECK`；越界写入仍交给 SP·数据库原有失败/重填流程，不增加运行时钳制层。
- 修复布尔字段初始值为 `true` 时 DDL 仍生成 `DEFAULT 0` 的通用问题。

## v0.3.3（2026-08-24）

- 修复使用“保存角色卡和模板到 SillyTavern”后，酒馆助手可能把原卡脚本面板状态延后回写到新卡，导致已删除的 `mvu` 引擎脚本以禁用状态重现、数据库桥被覆盖。现在创建后使用 ST 官方 `writeExtensionField` 按 `convertedAt` 精确命中新卡，再次固化转换后的 `tavern_helper`。
- 修复旧式内联状态栏在 `DOMContentLoaded` 时只执行一次 `await getChatMessages(...)[0].data` 的初始化竞态：转换时先等待数据库消息投影 shim 就绪。消息只读副本现同时投影实时 `stat_data` 和 `display_data`，避免残留的空 `display_data` 遮住实时状态；魔法棒“刷新转换卡前端”也可直接调用此类状态栏已识别的原生重读函数，不再只支持 `body.load` 整页前端。
- 修复开局分支合并时，外部 MVU/VWD 事件监听器交回旧式 `[当前值, 描述]` 叶子，导致 TEXT 列被写成 JSON 数组字符串并触发枚举 `CHECK IN` 失败。现仅对 layout 明确标记为 `pair` 的列在建表边界拆包，真实数组/JSON 业务字段保持不变。
- 修复 YAML 变量规则中 `${A|B}经验`、`${A|B}分区` 这类带前后缀模板键的 `type/range/check`丢失；非数值 `range` 数组现按枚举转换为表格强制约束和可选 DDL `CHECK IN`。
- 模板键展开出的同表列若拥有完全相同的范围、枚举、格式和 `check`，表格提示词现折叠为 `${露出|扩张}经验` 式合并规则；实际列、单列描述及每列 DDL 约束仍独立保留。
- 角色卡下拉使用最新 SillyTavern 上下文和列表指纹自动刷新，并增加手动刷新按钮；列表变动后按 `avatar+角色名`保留选择，不再沿用可能失效的数组下标。
- 新增可选转换配置。下载角色卡/模板或保存到 SillyTavern 时，按原卡名自动保存各表自动化参数与外部表来源；仅在同名配置冲突时请用户替换或重命名。同一转换连续下载/保存会绑定同一配置，内容未变时幂等跳过重写。配置可主动选择应用到更新版卡，也可删除。
- 外部表不保存副本，而记录“来源＋单表 UID＋表名＋结构指纹”；重用时从原来源取最新版。UID 只在来源内匹配且同时校验表名，最终合并仍以表名唯一并报告冲突/来源更新。
- 应用已有配置时先对新生成模板做匹配摘要；大部分配置表缺失、外部来源/表缺失或冲突时才请用户确认。拒绝后本次使用新卡基础模板，且产出时绝不覆盖被拒绝的配置。
- 删除外部来源中的“SP·数据库默认模板”：SP v8.9.2 内部有默认常量，但未通过公开 API 提供只读导出，服务器根路径的旧 JSON 也不保证存在。已存旧配置仍会安全尝试原引用，失败时明确提示且不回退为全局模板。

## v0.3.2（2026-08-23）

> 修复含 MVU 官方 `$meta` 的角色卡结构转换。此类卡需要用新版本重新转换并新开聊天。

### 官方 InitVar 元数据

- 按本地 MVU 官方源码的 `generateSchema` / `cleanUpMetadata` 顺序处理 `$meta`、`$arrayMeta` 与数组魔法标记 `$__META_EXTENSIBLE__$`：元数据只参与结构推导，不再生成 `$meta_required` 等错误业务表或 `$meta` 列。
- 支持 `extensible`、`recursiveExtensible`、`required`、`template` 以及根级 `strictTemplate`、`strictSet`、`concatTemplateArray` 的初始化解析；父级递归开放与数组元数据遵循官方代码的实际传播规则。
- `template` 可为初始为空的动态字典提供通用条目结构；多开场分支中的元数据也参与结构并集推导，显式固定对象不会再被文本启发式误判为动态字典。

### 表结构与数据往返

- 修复多层分类字典被合并到同一子表的问题；衣柜、服装状态、背包等同形但不同路径的容器现在按完整逻辑路径独立建表。
- 动态字典 `type` 中的索引签名名称（如 `[物品名称: string]`）现在作为行表业务键列名，不再一律显示为“键名”；若与 value 对象字段重名则自动消歧。
- 修复嵌套字段被状态栏扫描再次补成顶层同名列，以及子条目字段被错误提升到父表的问题。
- 混合字符串、数字、布尔值与 `null` 的标量字典改用 JSON 标量单元格，初始化、更新与新增时均保留原始类型，不再统一退化为字符串。
- 通配路径识别改为校验合法路径段，描述文本中的小数点不再产生虚假的人工核对警告。

### 规则提示

- YAML 规则中的 `${A|B}` 与 `${A/B}` 模板键会展开为完整逻辑路径，其下的 `type`、`check`、`range`、`format` 和枚举不再被跳过。
- 固定容器的表级 `check` 会按路径传播到实际承载数据的动态子表；MVU 的 `remove旧键+insert新键` 修改 value 措辞会转换为数据库行更新语义。
- 世界书清理不再把 `format_message_variable`、`get_message_variable` 或普通 `getvar(stat_data...)` 读取当成删除证据；带 `[mvu_update]` 的剧情机制、条件规则和格式指南会保留，只有 InitVar、结构更新规则及明确的写入/输出管道才整条删除。
- EJS 中完整字面路径的 `getvar('stat_data.组.字段')` 改用数据库安全路径函数：任意中间组缺失时返回 `undefined`，并保留 `{ defaults: ... }`，不再因生成连续属性访问而抛出 “Cannot read properties of undefined”；同时修复其他直接读取入口改写时可能误把字符串位置数字拼进 EJS 代码的问题。
- `{{format_message_variable::路径}}` 不再原样残留：世界书正文与表格 `note/check` 统一改为生成时从数据库取值；标量直接输出，对象/数组按 YAML 输出，并按 MVU 规则递归隐藏以 `$` 开头的键。
- 修复世界书 EJS/格式化宏无条件优先读取开局 checkpoint、偶发得到初始值的问题。统一读取顺序为“待写快照 → SP 提交回调 → 完整运行时表 → 持久化帧 → 结构骨架”；持久化缓存也改用内容指纹，不再把等长的新旧帧当成同一份数据。
- 填表插件单独调用 `EjsTemplate.evalTemplate(finalContent)` 时，数据库 helper 现在同时注册到所有同源 EJS 实例、注入 `prompt_template_prepare` 上下文，并对“包含转换器 helper 且未传 context”的直接 `evalTemplate` 显式调用 `prepareContext`。避免 helper `ReferenceError` 导致 SP 放弃整份 EJS 处理、把 `<%- mvu2shujuku… %>` 原样发给模型；普通 EJS 模板不介入。

### 脚本迁移

- 对可静态证明为“读取 chat 根变量、修改已知 MVU 顶层组、原对象写回”的旧镜像事务，自动改写为数据库 `Mvu.getMvuData/replaceMvuData`；普通 chat 脚本设置仍保留原作用域。
- 纯 Zod Schema 脚本即使把 `registerMvuSchema` 压缩改名导入，也会被识别并移除；含 `Mvu.*` 业务逻辑的混合脚本仍保留。
- 只在 mount 时读取一次 message `stat_data` 的内联 module 开场前端，会先等待数据库 `getVariables` shim 安装再启动，避免初始化竞态把衣柜、选项列表等状态永久固化为空。
- 兼容旧 MVU 状态栏直接读取 `getChatMessages(...)[0].data.stat_data` 的写法：返回消息的只读副本会投影当前数据库状态，不修改真实聊天记录；切换到非转换卡时恢复原始函数。

## v0.3.1（2026-08-22）

> 运行时健壮性与性能修复，无需重新转换卡片，刷新 SillyTavern 即可生效。

### 数据安全（切卡/切聊天）

- 修复写库重试链在切换聊天后重新捕获聊天标识、导致旧聊天待写快照可能写入新聊天的问题（同卡不同聊天同样受保护）。
- 修复初始化写入标志在重试期间泄漏到后续普通写入、可能绕过开局整表保护门禁的问题。
- 切换聊天时立即作废上一聊天的待写快照与挂起的合并写入，前端不再短暂读到旧聊天数据。
- 开局整表初始化在提交前后都校验聊天归属，切换窗口内不再误覆盖或误记账。
- 卡内数据桥（扩展未加载时的兜底路径）的合并写库补上同样的聊天切换守卫。

### 结构转换与表格生成

- 修复"下载角色卡"导入酒馆后变回原卡的问题：v3 卡 PNG 同时含 chara 与 ccv3 两个数据块，酒馆导入时优先读 ccv3，而此前回写只替换了 chara。现在回写会同步移除 ccv3 与重复块，任何读卡方都只能读到转换后的数据；同时转换输入改为与酒馆一致的 ccv3 优先读取，仅含 ccv3 的卡也能正常转换。

### 写入与初始化

- 修复布尔字段的开场回声写：数据库单元格把布尔存为 1/0，而快照提交的是 true/false，此前每轮都会把这批字段误判为差异并逐格重写一遍（表现为开场最终快照退化成数十次逐条写入）；现在 1↔true、0↔false 视为相同，真实的布尔翻转仍正常写入。
- 每次新写入拥有独立的就绪重试预算，修复一次失败耗尽预算后同聊天后续 `<UpdateVariable>` 楼层写入被静默放弃的问题。
- 单例表“默认值写回守卫”收紧为只拦整组全部变化都回到模板默认值的空读写回，不再吞掉重开一局、重置技能等合法重置操作。
- 空组保护扩展到关系子表：前端分批发送时不再把未加载的子表集合误判为“删除全部行”。
- 写库前的运行时就绪校验增加单例/JSON 表数据行的短时等待（最多 1.5 秒），物化完成前不再以空基线写入整组默认值；超时后退回表名校验并交由既有的补行/持久化对账守卫兜底，新聊天与切换开场分支的重载窗口不会再被永久阻塞。
- `initGameSession` 超时降级标记按聊天隔离，切换聊天后自动恢复完整初始化路径。
- 内存中的整表模板补行改用“最大行号 + 1”，修复删行后行号重复可能被插件拒收。

### 事件与状态

- 变量更新事件的去重指纹统一改为键序无关序列化，修复前端反复刷新的回声循环。
- 卡内桥的初始化事件监听句柄纳入共享注册表，扩展升级后不再累积多代监听器。
- 会话级按聊天记录的状态集合增加容量上限，长时间使用不再无界增长。

### 性能

- 前端读变量（`getVariables` / `getAllVariables`）增加投影缓存：同一份表格数据在写入、插件提交或切换聊天前不再每次全量反投影，大型状态栏与 EJS 卡的读取开销显著降低。
- 消息更新块扫描与首楼 `<initvar>` 轮询增加指纹缓存，未变化的楼层不再被每次事件和每 2 秒重复正则扫描。

## v0.3.0（2026-08-22）

> 以下内容为相较上一个公开版本 v0.2.13 的变化。

### 结构转换与表格生成

- 加强嵌套对象、动态字典、标量字典、数组和多层关系子表的转换，修复字段误入 `_扩展数据`、局部展开丢失字段及同名嵌套列对应错误等问题。
- 关系子表改用完整路径区分，并保留各层关联键，避免不同上级中的同名子项互相混淆。
- 展平列名统一用 `_` 表示层级；`${角色}.字段` 等可确定结构会尽量展开为列，只有无法预先确定键名的内容才保留在 `_扩展数据`。
- 修复 JSON 对象与数组混淆、布尔值被当成数字，以及数值默认值或特殊值触发 `CHECK` 约束失败等问题。
- 加强 `mvu_update` 中 YAML、Zod、TypeScript 和模板路径的识别，减少 EJS 局部变量、动态字典类型或子对象字段被误判为表格列的情况。

### 开局初始化与数据安全

- 当前开场分支中的 `<initvar>`、`<UpdateVariable>` 和 `<JSONPatch>` 会按原顺序参与初始化，避免切换开场白后变量块直接显示在正文或未写入表格。
- 大型开局数据改为一次性提交，减少逐格写入造成的长时间等待、重复日志和界面卡顿。
- 改进多开场白切换、重新生成、已有聊天和首楼替换场景，避免重复初始化、只写入部分数据或旧快照覆盖新数据。
- 修复前端整对象回写时丢失字段、默认值反复写回以及无实际变化仍循环保存的问题。
- 加强角色卡与聊天的归属校验，降低切换卡片或聊天时误用其他会话数据的风险。

### 状态栏、EJS 与脚本兼容

- 自动填表完成后会通知状态栏刷新；对于不监听更新事件的旧式状态栏，可使用其已有刷新能力或魔法棒菜单中的手动刷新入口，无需重载整个楼层。
- 修复自动填表后立即读取仍得到旧数据、重新生成时状态栏挂载较晚，以及部分 `body.load(...)` 前端无法重新显示的问题。
- 修正 TavernHelper 不同变量作用域的分流，避免聊天、角色、全局或脚本变量被错误当成 MVU 表格数据。
- 完善 Zod 阶段事件兼容，使依赖事件修改更新结果的脚本能继续工作。
- 保留卡片中的 EJS 控制流，并避免动态输出或局部变量进入 DDL 和填表提示词。
- 填表提示词中的 SillyTavern 宏会在运行时解析，同时兼容部分仅作用于世界书提示词的正则。
- 修复动态正则、Ticket 标记和自定义标签渲染在转换后失效或内容暴露到正文的问题。

### 规则提示与卡片内容保留

- 将提示词中的 JSON Patch 操作说明转换为数据库的新增、更新和移除语义，并兼容 SP 原生模式与 SQLite 模式。
- `_` 前缀内部字段不会交给 AI 直接维护，但卡片脚本和 EJS 仍可正常读写。
- 调整删除记录的提示条件，减少 AI 因角色暂时离场或状态变化而过早删除数据。
- 收紧转换时的清理条件，避免误删业务脚本、动态正则、剧情世界书、EJS 和显示渲染规则。
- 混合内容、简写 `mvu_update` 与无法确认用途的外部脚本会优先保留，并在转换结果中提示人工复核。
- 保留只负责折叠或美化的显示正则，仅移除能够确认已被数据库功能替代的变量处理部分。

### 操作改进

- 表格选择和转换结果中可展开查看列名。
- 魔法棒菜单增加“刷新转换卡前端”，用于手动刷新不支持自动监听的旧式状态栏。

### 升级提示

- 为获得新的表结构、规则转换和脚本清理结果，建议使用 v0.3.0 重新转换原 MVU 卡，并从新聊天验证初始化。
- 仅属于扩展运行时的修复可在刷新 SillyTavern 后对已转换卡生效，但旧卡不会自动获得新布局、模板和卡内清理结果。
- 转换后请核对转换报告中的“需人工处理”和“警告”；复杂外部脚本与动态 EJS 仍需实机测试。
